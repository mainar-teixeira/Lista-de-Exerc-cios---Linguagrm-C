#include <stdio.h>

int main() {

    float n1 = 7.0,
    n2 = 6.5,
    n3 = 9,
    mediaPonderada = (n1 * 1 + n2 * 2 + n3 * 3) / 6;

    printf("A média ponderada é: %.1f", mediaPonderada);

    return 0;
}