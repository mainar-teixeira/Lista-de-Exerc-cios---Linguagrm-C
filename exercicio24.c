#include <stdio.h>

int main() {

    int hora = 1,
    minutos = hora * 60,
    segundos = minutos * 60;
    
    
    printf("Equivalente a %d minutos ou %d segundos", minutos, segundos);

    return 0;
}